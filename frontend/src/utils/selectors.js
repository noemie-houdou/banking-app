export const userToken = (state) => state.login.userToken;
export const loginError = (state) => state.login.error;
export const profileInfos = (state) => state.profile.profileInfos;
export const newUsername = (state) => state.edit.newUsername;
